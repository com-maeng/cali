{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Gradle Build (Continuous)",
      "type": "shell",
      "command": "./gradlew build --continuous",
      "group": "build",
      "problemMatcher": [],
      "options": {
        "cwd": "${workspaceFolder}/backend"
      }
    }
  ]
}
